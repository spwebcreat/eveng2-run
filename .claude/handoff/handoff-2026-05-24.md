# Handoff 2026-05-24（G2 案件視点）

本ファイルは G2 Run HUD 案件の作業履歴。
本社全体の終会記録は `<本社>/.claude/handoff/handoff-2026-05-24.md` を参照（朝会・支店報告・改善候補等を含む全体俯瞰）。

## セッション概要

- **期間**: 2026-05-24 深夜 00:30 頃 着手 〜 02:30 過ぎ 終会
- **主軸**: G2 Run HUD MVP 一気通貫実装（発注プロトコル v3 体制 5 ステップ完走 + 実機表示成功）
- **状態**: MVP 実機表示成功・git init 5/25 持ち越し・GPS 精度検証は 5/25 実走テスト予定

## 本日の流れ

### Step 1: §21 公式 docs 調査

仕様書 §21 の 8 項目を本社で先に調査。Even Hub 公式ドキュメントは React/Next.js SPA で WebFetch / scrapling fetch が初期 HTML タイトルしか取れず、最終的に GitHub API で `LesenmiaoYu/evenhub-templates` の minimal / text-heavy 実コードを直接取得して SDK API map を完全把握。

主要発見：
- 公式テンプレ実体: `LesenmiaoYu/evenhub-templates`（旧資料 `even-realities/...` は古い）
- Node v20+ 必須・SDK `@evenrealities/even_hub_sdk` ^0.0.10
- 表示 576×288 px / 4-bit 緑モノクロ 16 階調
- SDK API: `waitForEvenAppBridge` / `createStartUpPageContainer` / `textContainerUpgrade` / `onEvenHubEvent` / `shutDownPageContainer`
- イベント振り分け: `event.sysEvent`（tap/lifecycle）/ `event.textEvent`（scroll）
- 罠: CLICK_EVENT(0) は Protobuf zero-value omit で undefined 到来 → `?? 0` coalesce 必須
- 制約: **QR サイドロードでは GPS 動かず、Hub アップロード後のみ動作**

### Step 2: 受入基準確定

10 項目を確定し旦那様 EA で認識合わせ：

1. Vite dev サーバ起動 → ブラウザでコンパニオン UI 表示
2. EvenAppBridge 経由で `createStartUpPageContainer` 呼び出し成功
3. テストモード ON でブラウザ上で距離・ペース・経過時間が 1 秒ごと更新
4. Start / Pause / Reset が G2 tap / コンパニオン UI で動作
5. 1km 到達でラップ画面が 5〜8 秒表示 → 通常 HUD 復帰
6. ランダムメッセージ 10 候補（日本語）からラップ時に選出
7. HR 欄は `HR -- bpm` 固定表示
8. テストモード OFF で navigator.geolocation.watchPosition 経路に切替
9. 旦那様向け Hub アップロード手順書 `docs/HANDOFF.md` を作成
10. `pnpm build` exit 0 / 型エラーなし

### Step 3: dev-engineer 実装発注

並列ではなく単独発注（コンポーネント間型依存強）。`/Apps/EvenG2/even-g2-run/` 直下に minimal テンプレ展開 → 19 ファイル作成。スモークテスト 30/30 PASS。

### Step 4: Codex 軽量 3 軸並列レビュー

`codex exec --skip-git-repo-check` で 3 並列発射（軸 A 実装品質 / 軸 B SDK 準拠 / 軸 C アーキ）。

集計（ユニーク化後）：FAIL 8 件 / WARN 9 件 / NIT 4 件
採用: FAIL 8 / WARN 6 / NIT 3（残 WARN 3 件は improvement-candidates 送り）

### Step 5: dev-engineer 修正サイクル

SendMessage で同 dev-engineer セッション継続発注。FAIL 8 件 + WARN 6 件 + NIT 2 件修正。NIT 1 件（N2 `shutDownPageContainer(1)` を `CONTAINER_ID` に統一）は dev-engineer が SDK 実物確認 → 引数は `exitMode` で Codex 誤指摘と判定 → 実装変更不要。スモーク 65/65 PASS。

主な FAIL 修正：
- F1 app.json permissions 欠如（location 追加）
- F2 Promise.race late resolve（bridge 2 段分離）
- F3 pause/resume 距離加算 bug（lastPosition クリア + baseline 扱い）
- F4 GPS モード `gps-waiting` 遷移未実装
- F5 CLICK_EVENT zero-value coalesce 未実装
- F6 render Promise 捨て + lastG2Content 早期更新
- F7 textContainerUpgrade false 戻り値を成功扱い
- F8 startGeolocation 同期 onError による再帰リスク

### 秘書直接修正 #1: pnpm pack 衝突

旦那様報告「`.ehpk` が生成されない」→ 原因: `pnpm pack` は pnpm built-in tarball コマンドで `scripts.pack` を無視する仕様（dev-engineer 懸念 #2 が現実化）。

修正:
- `package.json` の scripts キーを `pack` → `pack:ehpk` にリネーム
- `docs/HANDOFF.md` の該当コマンド記述も更新
- 余計に生成された `g2-run-hud-0.1.0.tgz` 削除
- `pnpm pack:ehpk` で `g2-run-hud-0.1.0.ehpk`（38,044 bytes）生成成功

### 秘書直接修正 #2: テストモードで km が動かない

旦那様報告「テストモードで km 動かん」→ 原因: `startMockRun` の同期 callout で `mockHandle = startMockRun(...)` の代入完了前に再帰起動が起き、setInterval が多重に走って同一位置で onPosition が連発 → haversine 距離が 0 で距離加算が止まる。**F8 同型バグの mock 側残存**。

簡易再現スクリプトで 100 回再帰起動を確認。修正:
- `src/mock/dev-mock.ts`: 初期位置発火を `queueMicrotask` に逃がす + `stopped` フラグ
- `src/main.ts`: `mockStarting` sentinel 追加で再入防止（F8 と同型の二重ガード）

修正後再現スクリプトで startMockRun 呼び出しが 100 → 1 回に正常化。

### Hub アップロード入力（旦那様作業）

- Category: Fitness / Sports 系（旦那様選択）
- About: 510 字の日本語紹介文（機能・操作・必要権限・MVP 制約を網羅）
- Tags: `running, fitness, HUD, GPS, pace`
- Access Permissions: **Location のみ ON**（最小権限）
- Data collection: **Location tracked のみ ON**（AI 機能は将来追加時に申請更新）

### 実機表示成功

旦那様が実機 G2 で HUD 表示確認 → **MVP 受入主条件達成**。
GPS 精度に懸念あり（民生 GPS 限界 + iOS WebView 制約の可能性）→ 5/25 Apple Watch 比較ランニングテスト予定。

## 旦那様確定事項

1. **実装先**: `/Apps/EvenG2/even-g2-run/` 直下（資料 md と同居）
2. **テストモード切替 UI** は本番ビルドにも残置・チェックボックス・ランタイム切替
3. **ダミーペース**: 1 秒ごと 2.78m（キロ 6 分相当）
4. **メッセージ・エラーは日本語**、HUD ラベル（RUN/LAP/AVG/km/bpm）は英語のまま
5. **git init + 初回 commit は 5/25 実走テスト後**（GPS 調整差分も含めて 1 コミット粒度）
6. **案件ルール配置**: G2 側に `CLAUDE.md` + `.claude/rules/learning/core.md` + `.claude/handoff/` を新設し、本社 dept/dev/findings/ を参照する構造。プロジェクトごとの関心の分離 + 段階的自立（過渡期は本社経由 / 安定期は G2 cwd 起動）
7. **AI ペースコーチング機能は将来フェーズで**（本社 improvement-candidates 登録・MVP 実運用 1 週間 + 心拍数取得後がトリガー）

## 修正コミット 0 件（git 未初期化）

本セッション内で EvenG2 側に commit は作成していない。5/25 実走テスト + GPS 調整後にまとめて git init + 初回 commit する。

## 未完了 / 次のステップ

### 5/25 朝以降
1. **Apple Watch 比較ランニングテスト**（GPS 精度懸念検証）
   - 観察項目: 総距離誤差 % / ペース応答性遅延 / 1km ラップ発火タイミング / 建物近く・林・トンネル / スマホ姿勢の影響
2. **テスト後の GPS パラメータ調整**
   - `src/run/state.ts` の `ACCURACY_GATE_M`（30m）/ `MAX_SANE_JUMP_M`（200m）/ currentPace 窓（直近 60 サンプル）
   - `src/run/geolocation.ts` の watchPosition オプション（既に enableHighAccuracy: true / maximumAge: 0 / timeout: 30000）
3. **git init + 初回 commit**
   - GPS 調整差分も含めて 1 コミット粒度
   - G2 側ファイル一式: src/ + index.html + app.json + package.json + tsconfig.json + vite.config.ts + README.md + docs/HANDOFF.md + even-g2-run-hud-handoff.md + CLAUDE.md + .claude/rules/learning/core.md + .claude/handoff/

### 将来フェーズ（本社 improvement-candidates 登録済）
- AI ペースコーチング機能（MVP 実運用 1 週間 + 心拍数取得後）
- PositionSource interface 注入（Apple Watch / 心拍数着手前）
- GPS フィルタ責務分離（テスト整備時）
- detectLap pure 化（テスト整備時）

## 既知の懸念事項

1. GPS 精度（実走テスト未実施・iOS WebView 制約の影響不明）
2. EvenAppBridge 5 秒 timeout が実機 Hub アップロード経路で十分か未検証
3. `evenhub pack` の動作詳細は実機未確認（CLI 単体での pack は確認済）

## 関連ファイル

### G2 案件内
- 実装: `src/` 配下 + `index.html` + `app.json` + `package.json` + `tsconfig.json` + `vite.config.ts`
- プロジェクト開発ルール: `CLAUDE.md`
- G2 固有 learning: `.claude/rules/learning/core.md`（4 件）
- 旦那様向け Hub アップロード手順書: `docs/HANDOFF.md`
- 仕様書: `even-g2-run-hud-handoff.md`

### 本社側（参考）
- 本社全体 handoff: `<本社>/.claude/handoff/handoff-2026-05-24.md`
- 本社 improvement-candidates: `<本社>/.company/secretary/improvement-candidates.md`
- 開発部 findings（横断ルール参照先）: `<本社>/.company/dept/dev/findings/`

## learning 4 件（G2 内 `.claude/rules/learning/core.md` 集約）

1. subagent 同型バグ棚卸し（F8 同型・mock 再帰バグから抽出）
2. Codex 指摘の実物検証（特に SDK / API シグネチャ系・N2 誤指摘事例）
3. pnpm / npm 組み込み名衝突回避（pack/publish/install/test 等を scripts に使わない）
4. SPA docs の代替取得（gh api で実コード直接取得）

## 次セッション着手手順

1. G2 案件再開時は本 handoff を確認
2. Apple Watch 比較ランニングテスト実施
3. GPS パラメータ調整
4. git init + 初回 commit

過渡期は本社経由作業継続。安定後（GPS 調整完了 + 数週間運用）は G2 cwd で Claude Code 起動 → G2 単独運用に移行検討。
