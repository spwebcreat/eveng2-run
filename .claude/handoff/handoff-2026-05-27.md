# Handoff 2026-05-27

## サマリ

even-toolkit (v1.7.2) 採用検討から始まり、Codex 批判レビュー 3 回 + 旦那様判断レビュー HTML 経由で v0.5+ ロードマップ確定（採用 30 機能 + 4 メタ / 却下 1 / 保留 1）。docs/ 整理 → 3 並列 spike (S1/S3/S4) 実装 → シオン QA + Codex 総合レビュー → P0 修正パケット化まで完遂。実機検証 ehpk 2 個を `spike-builds/` に配置済。

## 完了したこと

- **v0.5+ ロードマップ確定** — Codex 批判レビュー → SDK 実物検証 → 旦那様判断 UI HTML → SSOT 化
  - `docs/v0.5/decision-log.md` (SSOT)
  - 採用 30 機能 + exec 4 メタ判断
  - 却下 1: シューズ管理 + 距離追跡 (v0.8)
  - 保留 1: NO-GO リスト承認 → 「観察リスト」化 (`docs/v0.5/watch-list.md`)
- **docs/ 階層整理** — `v0.5/` (現フェーズ) / `archive/` (v0.4.0 履歴) / `design/source/` (Penpot)
- **3 並列 spike 実装** — Agent ツール直接呼び出し + worktree isolation
  - S1: 2×2 tile image 実機検証（46KB ehpk、シミュレータ起動 OK、実機 GO 未確定）
  - S3: even-toolkit + upng-js ビルド検証（text-clean は gzip 0.9KB、png-utils は pako 219KB 隠れ重荷）
  - S4: G2 マイク PCM ペアリング（47KB ehpk、16kHz/16-bit/100ms 公式仕様判明、実機 GO 未確定）
- **シオン QA cross-cut** — 私（リムル）が直接実施、P1 違反ゼロ確認
- **Codex 総合レビュー** — P0 5 件発見（cleanForG2 既存 HUD 破壊リスク等）
- **P0 修正パケット作成** — `docs/v0.5/dispatch/p0-fixes.md` (次セッション向け self-contained)
- **実機検証 ehpk 配置** — `spike-builds/g2-run-hud-spike-s1-0.5.0.ehpk` + `g2-run-hud-spike-s4-0.5.1.ehpk` (version 昇順、キャッシュ問題回避)
- **コミット 3 件** — `4216569` (v0.5+ 計画), `c1aac4a` (P0 パケット), `b58f63b` (version 昇順)

## 決定事項

- **NO-GO リスト = 観察リスト**：完全 NO-GO とせず SDK / Hub 進化を見て将来再検討
- **spike worktree は本流 merge 禁止**：spike branch で保管、main は無汚染維持
- **v0.5 本実装は P0 修正後着手**：image renderer は v0.6、S2 はその前必須
- **text-clean は cleanForG2 safe wrapper 経由で採用**：既存 HUD 記号 `▶◀●○` を保護
- **ehpk アップロード順は version 昇順**：S1 (0.5.0) → S4 (0.5.1)（learning P1 既知のキャッシュ問題回避）

## 未完了 / 次のステップ

### 🎯 旦那様タスク（手動）

1. **spike 実機検証**
   - 順序: S1 (v0.5.0) を先 → S4 (v0.5.1) を後
   - 手順: `docs/v0.5/spike-results/manual-verify.md`
   - 別 app_id (`com.spwebcreat.g2runhud.spike.s{1,4}`) で本番無汚染
   - 結果（GO/NO-GO + ログ）を秘書に共有
   - 検証完了後に Hub Portal の spike アプリは削除

### 🚀 次セッション開始時の指示

新しい Claude Code セッションを開いて以下を貼る:

```
docs/v0.5/dispatch/p0-fixes.md を読んで、P0-1 〜 P0-5 をすべて実施してください。
完了後、v0.5 本実装着手の準備が整ったことを報告してください。
context 節約のため、各 P0 は最小限の編集に留めてください。
```

P0 5 件 / 約 75 分作業:
1. cleanForG2 safe wrapper 作成（30 分、最重要）
2. v0.5-6 import 訂正 (`/glasses/text-clean` → `/text-clean`、upng-js 削除)
3. implementation-plan 前提条件修正（S2 不要を明記）
4. S1 worktree の `pack:ehpk` を `exit 1` 物理禁止
5. 旦那様向け報告書ドラフト

### ⏭ P0 修正完了後

- v0.5 本実装着手（Locked-phone QA / RendererPort / Settings / GPX schema v2 / HR 削除 / text-clean）
- S2 (image latency 計測) は v0.6 着手前に発注

## 関連ファイル

### SSOT
- `docs/v0.5/decision-log.md` — 判断ログ（採用 30 / 却下 1 / 保留 1）
- `docs/v0.5/watch-list.md` — NO-GO 保留の観察リスト
- `docs/v0.5/spike-plan.md` — spike 全体プラン
- `docs/v0.5/implementation-plan.md` — v0.5 本実装プラン

### 次セッション着手用
- `docs/v0.5/dispatch/p0-fixes.md` — P0 修正パケット（self-contained）
- `docs/v0.5/spike-results/manual-verify.md` — 旦那様向け実機検証手順

### Codex 総合レビュー材料
- `docs/.agent/codex-qa-summary-brief.md`
- `/tmp/codex-spike-total-review.md`（揮発性、要なら docs/v0.5/spike-results/ にコピー）

### spike 成果物（git ignored）
- `spike-builds/g2-run-hud-spike-s1-0.5.0.ehpk` (46KB)
- `spike-builds/g2-run-hud-spike-s4-0.5.1.ehpk` (47KB)

### spike worktree（commit 未実施、本流 merge 禁止）
- `.claude/worktrees/agent-a76eabf1014968b8f/` — S1 (`feat/v0.5-spike/s1-tiles`)
- `.claude/worktrees/agent-a748ac95112cd40e2/` — S3 (`feat/v0.5-spike/s3-toolkit-build`)
- `.claude/worktrees/agent-a51a4de47789f7530/` — S4 (`feat/v0.5-spike/s4-audio-pcm`)

## 引き継ぎ事項（次セッション必読）

1. **`cleanForG2` は既存 HUD を破壊する** — P0-1 最優先
2. **S2 (image latency 計測) は v0.5 不要 / v0.6 前必須** — 文書矛盾あり要訂正
3. **実機検証は旦那様の手動タスク** — 私から催促しない、結果待ち
4. **spike worktree の本流 merge は禁止** — branch 保管のみ
5. **本社 (my-company) peer の lve28ckt は使用不可** — メッセージは届くが受信者不在で処理されない
