# G2 Run HUD ロードマップ

> このファイルは人間向けロードマップのソースです。編集したら `docs/roadmap.html` を再生成してください（`/spec-to-html docs/roadmap.md docs/roadmap.html`）。
> 判断の経緯（なぜこの機能を採用・却下したか）の正本は `docs/v0.5/decision-log.md`、v0.5 の実装詳細は `docs/v0.5/implementation-plan.md` にあります。

最終更新: 2026-05-31 ／ 現在地: v0.5 実装中

---

## 最終目標

<!-- illust: 最終目標の全体像。走りながらグラスに距離とペースが映り、走り終わったらスマホで振り返る。左に G2 グラスをかけて走る人と頭上の HUD（距離・ペース・時間）、右にスマホで走行サマリを見る様子。下に text 表示 → 画像 HUD → 音声操作 → Strava 連携 と右へ育っていく矢印。 -->

走りながらグラスに距離・ペース・時間を映し、走り終わったらスマホで振り返る。
文字だけの表示から始めて、画像の HUD・音声での操作・Strava 連携まで備えた「ランニングの相棒」を v1.0 で完成させます。

いまは v0.4.0（文字 HUD）が実機で動いている段階です。ここから v1.0 まで、全 7 フェーズ・約 6 ヶ月（副業ペースで週 10〜15 時間）を見込んでいます。

| フェーズ | 期間目安 | ひとことで言うと | 状態 |
|---|---|---|---|
| v0.5-spike | 1 週 | 先行検証（画像表示・マイク） | 完了 |
| **v0.5** | **2 週** | **基盤固め（文字 HUD 改善 + 土台づくり）** | **実装中（6/6 実装済み・実機QA待ち）** |
| v0.6 | 3 週 | 画像 HUD（グラフィカル化） | 予定 |
| v0.7 | 1 ヶ月 | スマホ UI を React 化 | 予定 |
| v0.8 | 1 ヶ月 | ワークアウトのプラン作成・実行 | 予定 |
| v0.9 | 1 ヶ月 | 外部センサー・音声操作・Strava | 予定 |
| v1.0 | 1 ヶ月 | 走行後サマリ・オンボーディング・公開仕上げ | 予定 |

---

## いま: v0.5（基盤固め・実装中）

<!-- illust: いまの v0.5 で作るもの。中央に「基盤づくり」と書いた箱、そこから 6 本の枝が伸びる。枝の先に「ロック中でも正確（QA）」「表示の土台（描画ポート）」「設定画面」「GPX 書き出し」「心拍 UI を撤去」「文字のお掃除」の 6 つのアイコン。手前に「派手な新機能より、後で効く土台を固める回」とコメントする小さなキャラクター。 -->

文字 HUD の弱点を直しつつ、これからのフェーズで効いてくる「土台」を作るフェーズです。新しい見た目より、後から効く基盤を固める回と位置づけています。全 6 項目。

**進捗（2026-05-31 時点）: 6 項目すべて実装済み（ビルド + ブラウザ動作確認済み）。旦那様の実機 QA（R-1〜R-10 / L-1〜L-6）を通れば v0.5.0 リリースへ。**

状態の見方:
- **完了** = 実機 QA まで通って確定したもの
- **実装済み** = コードは完成し、ビルドとブラウザ動作確認まで済んだもの。旦那様の実機 QA（R-1〜R-10 等）で確認できたら「完了」に上げる
- **未着手** = これから着手

| # | 項目 | 状態 | 何のため |
|---|---|---|---|
| 1 | ロック中の挙動を強化（QA） | 実装済み | スマホをロックしても距離と時間がずれない |
| 2 | 描画ポートの抽象化 | 実装済み | v0.6 で画像 HUD に切り替えられるようにする |
| 3 | 設定画面を新設 | 実装済み | 単位・目標ペース・GPS 精度をスマホで設定 |
| 4 | GPX 書き出し | 実装済み | 走行ログを書き出して Strava 等に手動投稿 |
| 5 | 心拍 UI を撤去 | 実装済み | 取得できない心拍の「嘘表示」を物理削除 |
| 6 | 文字のお掃除 | 実装済み | 絵文字・非対応文字を表示前に除去してエラー予防 |

各項目の詳しい中身は折りたたみに入れています（正式な実装手順は `docs/v0.5/implementation-plan.md`）。

<details><summary>1. ロック中の挙動を強化（QA） ［実装済み・実機QA待ち］</summary>

スマホをロックしたりアプリを背面に回すと、iOS は数秒で JavaScript の実行を止めます。すると位置取得が止まり、経過時間と距離がずれます。Wake Lock の再取得・復帰時の時間補正・Service Worker 導入（オフライン審査対策）で、ロック 5 分／10 分／30 分でもずれない状態を目指します。完了条件は手動 QA で L-1〜L-6 と既存機能 R-1〜R-10 が全て合格すること。

実装メモ（2026-05-31）: tick を runTick() に抽出し画面復帰時に呼んで凍結中の経過時間を即時補正。Wake Lock を最大 3 回 retry に強化。復帰時ステータスを「GPS 復帰中…」に。public/sw.js を network-first（オンライン時は常に最新取得＝古い UI 残存を防ぐ）で新設。**実機 QA（L-1〜L-6 / R-1〜R-10、特に L-6 オフライン審査）は旦那様の確認待ち。**

</details>

<details><summary>2. 描画ポートの抽象化 ［実装済み・実機QA待ち］</summary>

いまの文字描画を `RendererPort` という共通の入り口の裏に隠し、v0.6 で画像の描画に差し替えられるようにします。v0.5 の時点では文字描画だけ完成させ、`?renderer=image` を指定したときは「未実装」と表示して文字描画にフォールバックします。既存機能は v0.4.0 と完全に同じ動作を維持します。

実装メモ（2026-05-31）: `src/even/renderer-port.ts`（port 定義）・`text-renderer.ts`（text 実装）・`select-renderer.ts`（URL クエリ選択）を新設し、`main.ts` を port 経由に切替。ビルドとブラウザ動作確認済み。

</details>

<details><summary>3. 設定画面を新設 ［実装済み・実機QA待ち］</summary>

単位（メートル法／ヤード法）・目標ペース・GPS 精度・表示モード初期値をスマホ側の折りたたみパネルに集約します。v0.7 で React 化する予定なので、v0.5 では素の HTML と最小限の JavaScript で作ります。設定は即時反映、再読み込み不要。

実装メモ（2026-05-31）: `src/settings/`（schema / persistence）を新設し companion に折り畳みパネルを追加。単位を `format.ts` に追加し描画全体に伝播（既定 metric で従来挙動維持）。ブラウザで単位切替が companion・グラス HUD プレビュー両方に即時反映され localStorage 永続化されることを確認。目標ペースは保存のみ（差分表示は v0.6）。

</details>

<details><summary>4. GPX 書き出し ［実装済み・実機QA待ち］</summary>

走行履歴を GPX 形式で書き出し、Strava 等へ手動投稿できるようにします。今の履歴には走行中の位置点が入っていないため、保存形式を v2 に上げて走行中に 5 秒ごとの位置点を記録する処理も含めます。古い v0.4.0 のデータは「サマリのみの GPX」として書き出せます。位置点は端末内のみに保存し、外部送信は手動書き出し時だけです。

実装メモ（2026-05-31）: 保存形式を schema v2 化（TrackPoint 追加）、走行中 5 秒ごとに位置点を記録（最大 10,000 点）。`src/export/gpx.ts` で GPX 1.1 に変換し、各履歴に書き出しボタンを追加。生成 GPX を xmlstarlet 相当（xmllint）で well-formed 確認済み。**Strava へのアップロード地図表示は実機での書き出し → 投稿で旦那様の確認待ち。**

</details>

<details><summary>5. 心拍 UI を撤去 ［実装済み・実機QA待ち］</summary>

SDK に心拍を取得する API が無いため、内蔵の心拍表示は実態のない「嘘 UI」です。グラス・スマホ両方から物理的に削除します。将来 v0.9 で外部 Bluetooth センサーから取る形で復活させる前提で、型の予約枠（`externalHr`）と表示枠だけ残します。

実装メモ（2026-05-31）: グラス container を 8 個から 7 個に削減し、`render.ts` / `bridge.ts` / `state.ts` / `main.ts` / スマホ側 `index.html` から心拍表示を物理削除。container ID 15 と型 `externalHr` を v0.9 BLE 用に予約。ブラウザでグラスプレビューと計測データから心拍が消えたことを確認済み。

</details>

<details><summary>6. 文字のお掃除（サニタイズ） ［実装済み・実機QA待ち］</summary>

絵文字や未対応の文字が表示処理に渡る前に取り除き、SDK のエラーを予防します。既存の `cleanForG2Safe` 安全ラッパー経由で適用します（`even-toolkit` の素の `cleanForG2` は `▶◀●○` や前後の空白まで壊すため直接使わない）。`RUN ▶` や全角スペースの中央寄せなど、既存表示は変えません。

実装メモ（2026-05-31）: `bridge.ts` の描画チョークポイント（全 text container の送信直前）で `cleanForG2Safe` を適用。安全ラッパーは自己完結実装のため `even-toolkit` 依存は追加せず。ブラウザで `▶ ● ○` が壊れないことを確認済み。

</details>

実装順序の目安は、第 1 週で「描画ポート + 心拍撤去」をまとめて、続けて「ロック中 QA」。第 2 週で「設定画面 → GPX → 文字のお掃除」、最後に統合テストと v0.5.0 リリース準備。

---

## 次: v0.6（画像 HUD）

文字表示から画像ベースの HUD へ進化させるフェーズ。先行検証 S1（2×2 タイルでの画像表示）は実機で合格済みです。着手前に S2（画像更新の遅延計測）の実施が必須です。

<details><summary>v0.6 の 5 項目を見る</summary>

- スプラッシュ画面のグラフィカル化
- ペースの推移グラフ（ラップ完了時に更新）
- ミニマップ（5 秒ごと更新）
- 公式の 191 アイコン採用
- ラップサマリのグラフ化

着手前タスク: S2（画像更新の遅延計測。1Hz／状態変化のみ／200ms の 3 パターンを各 30 分連続で計測）。

</details>

---

## その先: v0.7 → v1.0

<details><summary>v0.7（スマホ UI を React 化・1 ヶ月）</summary>

- React + Vite + `even-toolkit/web` 導入（スマホ UI のみ。グラス側は対象外）
- Calendar／LineChart／Timeline／StatGrid といった表示部品
- 履歴の保存先を IndexedDB へ移行
- グラス画面のルーター化（多画面対応）

</details>

<details><summary>v0.8（ワークアウトのプラン作成・実行・1 ヶ月）</summary>

- スマホでワークアウトのプラン作成（ScrollPicker）
- グラスでプラン実行（タイマー表示）
- 目標ペースとの差分を大きく表示

却下: シューズ管理・走行距離追跡（v1.0 以降も復活させない。再検討する場合は明示の指示が必要）。

</details>

<details><summary>v0.9（外部センサー・音声操作・Strava・1 ヶ月）</summary>

- Web Bluetooth で外部心拍センサー連携
- Strava OAuth + 書き出し（バックエンドのプロキシ経由）
- G2 マイクでの音声コマンド（"pause" / "lap" / "next"）。先行検証 S4（マイク PCM ストリーム）は実機で合格済み
- 走行後の音声メモ

</details>

<details><summary>v1.0（仕上げ・公開・1 ヶ月）</summary>

- Claude API のバックエンドプロキシで走行後サマリ生成
- スマホスピーカーでの音声通知
- オンボーディングフロー
- Hub Portal 提出まわりの整理

</details>

---

## アーカイブ

普段は見なくてよい確定事項です。畳んでおきます。

<details><summary>完了済み: v0.5-spike（先行検証 4 件）</summary>

| 検証 | 内容 | 結果 |
|---|---|---|
| S1 | 2×2 タイルで 576×288 の画像を実機表示 | 合格（v0.6 画像 HUD 継続可） |
| S2 | 画像更新の遅延計測 | 未着手（v0.6 着手前に必須） |
| S3 | `even-toolkit` のビルド検証 | 採用確定（`cleanForG2Safe` 経由） |
| S4 | G2 マイクの PCM ストリーム | 合格（5 分 / n=2979 / 31,776 B/s = 理論値ど真ん中。v0.9 音声操作へ） |

検証の途中で「先行検証アプリのはずが本番 UI が起動する」事故が発生し、Codex の第二意見とクリーンビルド方針・パッケージ前の検査ゲートで再発防止を確立しました（詳細は `.claude/rules/learning/core.md`）。

</details>

<details><summary>保留中: 観察リスト（公式の対応待ち・6 件）</summary>

「諦めた機能」ではなく「観察リスト」として別管理します。Even Hub SDK のリリースノートを節目ごとに確認し、対応 API が追加されたら再評価します（管理ファイル: `docs/v0.5/watch-list.md`）。

- G2 スピーカーでの読み上げ（SDK に音声出力 API が出たら）
- Spotify / Apple Music の操作（音楽制御 API が出たら）
- ターンバイターン・ナビ（地図 SDK／地図タイル配信が出たら）
- 通知転送（SMS / LINE 等。スマホ通知 API が出たら）
- ソーシャル機能（運用コストの問題が解決したら。v2 以降）
- 内蔵心拍センサー（v0.9 で外部 Bluetooth による代替を採用済み。本体内蔵が出ても上書きしない）

</details>

<details><summary>却下: シューズ管理・走行距離追跡</summary>

優先度判断により却下。v1.0 以降も復活させません（再議論したい場合は旦那様から明示の指示）。

</details>

<details><summary>判断の経緯アーカイブ（旧・判断レビュー画面）</summary>

採用 30 機能 + メタ判断 4 件を一件ずつ採用／却下／保留で判断した当時の画面は `docs/v0.5/roadmap-review.html` に残してあります。判断はすでに `docs/v0.5/decision-log.md` に確定記録済みのため、経緯の参照用です。

</details>

---

## このロードマップの更新ルール

- 各フェーズの状態が変わったら、本ファイル（`docs/roadmap.md`）を編集して `docs/roadmap.html` を再生成する
- フェーズ着手前には、そのフェーズ用の mini decision-log と mini implementation-plan を新規作成する（`docs/v0.6-decision-log.md` 等）
- フェーズ着手時には観察リスト（`docs/v0.5/watch-list.md`）を読み返し、復活させる機能が無いか確認する
